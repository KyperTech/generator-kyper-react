import React, { Component, PropTypes } from 'react'

export default class <%= name %> extends Component {
  constructor (props) {
    super(props)
  }

  static propTypes = {

  };

  render () {
    return (
      <div>

      </div>
    )
  }
}
