import React, {Component, PropTypes} from 'react';

class <%= name %> extends Component {
  constructor(props){
    super(props);
  }
  render(){
    return (
      <div>

      </div>
    );
  }
}

export default <%= name %>
