import React, {Component, PropTypes} from 'react';
import './<%= name %>.scss';

class <%= name %> extends Component {
  constructor(props){
    super(props);
  }
  render(){
    return (
      <div className="<%= name %>">

      </div>
    );
  }
}

export default <%= name %>
